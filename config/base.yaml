dataset: "Gustavosta/Stable-Diffusion-Prompts"
seed: 42
device: ???

pipeline:
  model_id: "stabilityai/stable-diffusion-2-1-base"
  guidance_scale: 3.0
  num_inference_steps: 50
  image_width: 512
  device: ${device}
  solver_order: 0

exp_name: "no_wm"
base_path: "./images/sdp/ddim"
output_path: "${base_path}/${exp_name}"
no_wm_path: "${base_path}/no_wm"
